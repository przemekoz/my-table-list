import { PaymentDetailDialogComponent } from './payment-detail-dialog.component';

describe('PaymentDetailDialogComponent', () => {
    it('should create', () => {
        const component = new PaymentDetailDialogComponent({ data: '' });
        expect(component).toBeTruthy();
    });
});
